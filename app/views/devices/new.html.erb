<h1 class="mb-5">デバイスの新規登録</h1>
<%= form_for @device do |f| %>
  <%= render "shared/validation_errors", f: f %>

  <% if @error_message %>
    <div class="alert alert-danger">
      <%= @error_message %>
    </div>
  <% end %>

  <div class="mb-3 row">
    <%= f.label :uuid, "デバイスID", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= f.text_field :uuid, class: "form-control", required: true %>
      <div class="form-text">
        pbmの実行ディレクトと同じディレクトにある `device_id` という名前のファイルの中身を入力してください
      </div>
    </div>
  </div>
  <div class="mb-3 row">
    <div class="col-sm-0"></div>
    <div class="col-sm-12">
      <%= f.submit "登録する", class: "form-control btn btn-primary" %>
    </div>
  </div>
<% end %>
